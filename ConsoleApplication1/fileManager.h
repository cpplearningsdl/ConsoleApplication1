#pragma once
#include <string>
#include <vector>
#include <SDL3/SDL_surface.h>

class fileManager {
public:
	// Text file interface
	static std::string openTextFile(const std::string& filePath);
	 
};
